name: ASTRA CI
on:
  push:
    paths: ['ASTRA/**', '.github/workflows/astra.yml']
  pull_request:
    paths: ['ASTRA/**', '.github/workflows/astra.yml']
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GKSwstype: '100'
    steps:
      - uses: actions/checkout@v7
      - uses: julia-actions/setup-julia@v2
        with:
          version: '1.10'
      - uses: julia-actions/cache@v2
      - run: julia --project=ASTRA -e 'using Pkg; Pkg.instantiate(); Pkg.test()'
